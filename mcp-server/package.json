{
  "name": "governed-golden-path-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server exposing this repo's deployment/policy/CVE state as agent tools, grounded in graph/seed-data.json.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "smoke-test": "npm run build && node smoke-test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.9.3"
  }
}
